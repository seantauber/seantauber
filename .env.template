# OpenAI Configuration
# API key for GPT-4 access, required for content analysis and README generation
OPENAI_API_KEY=your_openai_api_key

# Gmail Configuration
# Path to Google Cloud credentials JSON file
GMAIL_CREDENTIALS_PATH=/path/to/google-credentials.json
# Path to Gmail API token storage
GMAIL_TOKEN_PATH=/path/to/token.json
# Label to monitor for newsletters
GMAIL_LABEL=GenAI-News

# GitHub Configuration
# Personal access token with repo scope
GITHUB_TOKEN=your_github_token

# Storage Configuration
# Path to SQLite database file
DATABASE_PATH=/path/to/database.sqlite
# Path to vector storage directory
VECTOR_STORE_PATH=/path/to/vector/store

# Logging Configuration
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional Monitoring Configuration
# Token for LogFire monitoring (if used)
# LOGFIRE_TOKEN=your_logfire_token

# Rate Limiting
# Maximum API requests per component (optional, defaults in pipeline_config.yaml)
# GITHUB_MAX_REQUESTS=5000
# OPENAI_MAX_REQUESTS=60

# Notes:
# 1. All paths should be absolute or relative to project root
# 2. Ensure proper permissions for all file paths
# 3. Create directories for DATABASE_PATH and VECTOR_STORE_PATH if they don't exist
# 4. Keep this file secure and never commit actual credentials
# 5. See config/pipeline_config.yaml for additional configurable settings
